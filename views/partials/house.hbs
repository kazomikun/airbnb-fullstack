<div class="row row-cols-4">
{{#each houseArray as |house|}}
<div class="col mb-3">
	<div class="card">
		<img
			src="{{house.photos.[0]}}"
			alt=""
			class="card-img-top"
		/>
		<div class="card-body">
			<span>{{house.location}} • {{house.rooms}} Rooms</span>
			<h5 class="card-title">
				<a href="/houses/{{house._id}}">{{house.title}}</a>
			</h5>
			<div class="row">
				<div class="col">1 Reviews</div>
				<div class="col text-end">${{house.price}}/night</div>
			</div>
		</div>
	</div>
</div>
{{/each}}
</div>
